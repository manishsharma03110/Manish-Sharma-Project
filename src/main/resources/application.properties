spring.application.name=portfolio

# Plain Maven/wrapper and IDE launches use the self-contained local database.
# Deployments must opt in to prod and supply DATABASE_URL, DATABASE_USERNAME,
# and DATABASE_PASSWORD. An explicit active profile overrides this default.
spring.profiles.default=local
